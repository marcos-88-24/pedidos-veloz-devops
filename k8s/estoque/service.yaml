apiVersion: v1
kind: Service
metadata:
  name: estoque-service
spec:
  selector:
    app: estoque
  ports:
    - protocol: TCP
      port: 3003        # porta interna do serviço
      targetPort: 3003  # porta do container
      nodePort: 30003   # porta exposta no Node
  type: NodePort
