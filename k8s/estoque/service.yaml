apiVersion: v1
kind: Service
metadata:
  name: estoque-service
spec:
  selector:
    app: estoque
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 31003
  type: NodePort
